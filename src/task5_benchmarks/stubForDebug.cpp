// Используется только для обхода ограничений CMake в Debug конфигурации
#include <iostream>

int main() {
  std::cout << "This executable is a stub. "
               "To run the real task5_benchmark, "
               "use the release build."
            << std::endl;
  return 0;
}
